// Novo recurso do ES2015

const pessoa = {
    nome: "Ana",
    idade: 5,
    endereco: {
        logradouro: "Rua ABC",
        Rua: 1000
    }
}

const idade = pessoa.idade;
console.log(`A idade da ana é de ${idade} anos.`);