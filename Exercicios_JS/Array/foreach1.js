const aprovados = ["Lucas","Bárbara","Letícia","Kaio"];
aprovados.forEach((el,indice,array)=>{
    console.log(`${indice+1} -> ${el}`);
    console.log(array);
});

aprovados.forEach(el=>{
    console.log(el);
})